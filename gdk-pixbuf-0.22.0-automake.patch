--- gdk-pixbuf-0.22.0/configure.in.automake	2011-12-06 10:48:39.348525418 +0100
+++ gdk-pixbuf-0.22.0/configure.in	2011-12-06 10:49:12.588525034 +0100
@@ -18,6 +18,7 @@ AC_SUBST(GDK_PIXBUF_MICRO)
 AC_SUBST(GDK_PIXBUF_VERSION)
 AM_PROG_LIBTOOL
 
+AM_PROG_AS
 AC_PROG_CC
 AC_ISC_POSIX
 AM_PROG_CC_STDC
--- gdk-pixbuf-0.22.0/doc/Makefile.am.automake	2011-12-06 10:54:59.588524580 +0100
+++ gdk-pixbuf-0.22.0/doc/Makefile.am	2011-12-06 10:55:01.618526531 +0100
@@ -156,7 +156,7 @@ install-data-local:
 	  done; \
 	fi)
 #	  echo '-- Installing $(srcdir)/html/index.sgml' ; \
-#	  $(INSTALL_DATA) $(srcdir)/html/index.sgml $(DESTDIR)$(TARGET_DIR); \
+#	  $(INSTALL_DATA) $(srcdir)/html/index.sgml $(DESTDIR)$(TARGET_DIR); 
 
 #
 # Require gtk-doc when making dist
--- gdk-pixbuf-0.22.0/gdk-pixbuf/Makefile.am.automake	2011-12-06 11:05:04.858525909 +0100
+++ gdk-pixbuf-0.22.0/gdk-pixbuf/Makefile.am	2011-12-06 12:13:55.581025587 +0100
@@ -116,9 +116,9 @@ BMP_LIB = libpixbufloader-bmp.la
 
 XBM_LIB = libpixbufloader-xbm.la
 
+libexecmoddir = $(libexecdir)
 if BUILD_DYNAMIC_MODULES
-
-libexec_LTLIBRARIES = 	\
+libexecmod_LTLIBRARIES = 	\
 	$(PNG_LIB)	\
 	$(JPEG_LIB)	\
 	$(GIF_LIB)	\
@@ -134,7 +134,7 @@ libexec_LTLIBRARIES = 	\
 extra_sources = 
 
 else
-libexec_LTLIBRARIES = 
+libexecmod_LTLIBRARIES = 
 
 extra_sources = $(libpixbufloader_png_la_SOURCES) 	\
 		$(libpixbufloader_jpeg_la_SOURCES) 	\
